class Hotel {
  int id;
  String image, name, description, rating, price, jumlah;

  Hotel({
    required this.id,
    required this.image,
    required this.name,
    required this.description,
    required this.rating,
    required this.price,
    required this.jumlah,
  });
}
