class User {
  final int? id;
  String? username, email, password, gender, noTelp, origin;

  User({
    this.id,
    this.username,
    this.email,
    this.password,
    this.gender,
    this.noTelp,
    this.origin,
  });
}
